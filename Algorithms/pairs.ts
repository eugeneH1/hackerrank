const paired = (ints: number[], s: number): [number, number] | void => {
  let pairs: Record<number, number>;
  for (let i = 0; i < ints.length - 1; i++) {
    for (let j = i + 1; j < ints.length; j++) {
      pairs[i] = 
    }
  }
}
